// Support structure material vertex shader.
// The sprite rect and texture info are emitted from the instance in
// flw_instanceVertex (shared instance shader); nothing more to do here, but
// this entry point must exist for the material slot.

void flw_materialVertex() {
}